-- Enable neoconf
--
-- Neoconf is supported in AstroNvim, however only as an optional dependnecy
return {
  "folke/neoconf.nvim",
  cmd = "NeoConf", -- Also add a command invocation support for pre-load
}
